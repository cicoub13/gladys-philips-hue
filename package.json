{
  "name": "gladys-philips-hue",
  "version": "1.1.0",
  "private": true,
  "description": "Philips Hue external integration for Gladys Assistant (lights: on/off, brightness, color, white temperature).",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "lint": "eslint .",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "test": "node --test --test-timeout=30000",
    "test:coverage": "node --test --test-timeout=30000 --experimental-test-coverage"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@gladysassistant/integration-sdk": "^0.9.0"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.7.0",
    "eslint-config-prettier": "^10.1.8",
    "globals": "^17.7.0",
    "prettier": "^3.9.5"
  },
  "license": "Apache-2.0"
}
